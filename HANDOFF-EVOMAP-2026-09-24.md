# EvoMap 当前演示交接 · 2026-09-24

## 主版本与保留方案

- `main`：项目唯一主版本，统一前端、后端和硬件接入；当前交互参照 `lirc0618/EvoMap@63dd7ec`。保持粉色App，用EvoMap旧按键及任务创建时采集。
- `future-iteration`：昨天完整代码原样保留，提交 `f2a8e3e4d764671b79ed7fc78154917010b4ed08`。连续句末上传、每5秒自动识别和原单/双/三击方案留待后续。

两个外部仓库另存为 `reference-evomap`（参考）和 `history-backend`（历史参考），均不覆盖主版本。见 [分支说明](BRANCHES.md)。

## 当前链路

创建时录音（最多15秒，结束后处理）→ 电脑本地Whisper转文字 → 可选千问描述当前单张照片 → DeepSeek拆原子动作 → 统一数据库 → 粉色App展示全部步骤。

任务中不持续录音或定时看图、不自动识别步骤完成；App任意勾选，硬件按键处理当前步骤，两者同步。没有增加持续录像或视频文件上传。

| 状态 | 操作 | 结果 |
|---|---|---|
| 待机 | K1单击 | 开始录音 |
| 录音中 | K1单击 | 停止，提交任务；15秒也会自动停止 |
| 待机 | K2长按 | 恢复最近暂存任务 |
| 执行中 | K1单击／双击／长按 | 完成当前步骤／撤回一步／暂存并新建 |
| 执行中 | K2单击／双击 | 缩小当前动作／换一种方法，更新App同一步骤 |
| 完成页 | K1单击／K2单击 | 回到新建待机／重做最后一步 |
| 生成中 | 任意按键 | 拒绝改变进度，等待结果 |

## 硬件接入

使用EvoMap原固件，nRF直接连接Mac USB，运行：

`.venv/bin/python scripts/hardware_gateway.py --serial /dev/cu.实际USB串口`

LRCP摄像头接Mac，在App选择摄像头并勾选画面辅助；只在创建任务/硬件录音期间取图。网页保持打开。也可给网关 `--camera-url` 使用设备真实JPEG接口。暂不提供S3脱离电脑独立运行。

马达固定节奏及灯光能力尚需实体驱动和回执验证；本次未改动该部分，不把基础短震动冒充已完成整组提醒。

## 验证记录

- 后端52项、前端7项测试通过，含模拟USB按键→停止录音→创建任务→完成同一App步骤，以及不在录音过程中上传、不自动重启录音。
- 真实模型：已有合成语音→本地Whisper→DeepSeek创建成功，约3.01秒；修正视觉提示后，测试图片→千问可见事实→DeepSeek步骤成功，模型合计约2.44秒。样本延迟不包含用户录音时间或实体硬件传输。
- 真实API完成、撤回、暂存、新建、恢复、换方法通过，测试数据仅写隔离4180库。4173正式服务已重启，确认默认evomap、录音关闭、无连续摄像头会话。
- 本次浏览器自动化工具无法挂接测试页，未完成新版页面的浏览器操作验收；前端语法及录音流程自动测试已通过。
- 实体设备未接入，仍需现场验证USB串口、真实摄像头权限/取图、按键及马达。软件通过不代表实机通过。
