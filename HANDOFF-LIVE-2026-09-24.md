# MindLoop 晨间交接 · 2026-09-24

**软件链路已用真实模型跑通；实体摄像头、马达尚未接到本机，接实物后继续验证。**

## 可以同步给团队的改动

1. 保留粉色 V2，接统一后端。首次拆成原子动作，App 展示全部步骤，可任意勾选/撤销；AI 可一次确认多个步骤，每完成一小步计一次，刷新/重启保留。
2. 增加本地“快处理”：连续 PCM → VAD 句末约0.6秒切句 → 本地 Whisper → 云端 DeepSeek。每句话尽快处理，30秒只是单段上限，不是固定等待；本地模型运行在演示电脑上。
3. 增加每5秒摄像头取图、真实视觉理解和步骤同步，忙时保留最新画面。持续动作另验时间证据，静态文档不能被当作已经写了两分钟。
4. 补齐提醒时间弹窗、每日重复、编辑/推迟/取消；未完成待办顺延。偏离按演示规则、回归只认最近一次，跨日按北京时间记录。
5. 增加 USB 音频/按键与摄像头 JPEG 网关；连续录音和三击固件修改稿；震动排队、设备能力检查、领取、成功回执、超时处理。没有硬件回执就不显示“已振动”。
6. 修复迟到识别覆盖手动更正、旧语音重复建提醒、关闭摄像头后仍写入、浏览器缓存旧代码、录音末包丢失等问题。

## 实测

- 真实链路：App 输入 → DeepSeek拆解 → 全部步骤显示；合成连续语音 → 本地ASR → DeepSeek → 一次勾选2步 → App刷新仍为2/3；文档测试画面 → 真实视觉API → 可见步骤自动勾选，持续步骤保持未完成。
- 句末到结果：约 **3.7～6.5秒**，最后一次 **5.1秒**（本地ASR1.26秒、云端3.23秒、断句0.6秒）。不是同声传译级即时反应。
- 最终视觉模型 `Qwen3-Omni-30B-A3B-Instruct`：三个软件链路样本约 **1.6 / 1.8 / 2.0秒**，不含最多5秒采样等待、App最多约1秒刷新和实体马达。此前其它视觉配置出现过41秒及超时，供应商延迟仍会波动，不能保证准点振动。
- 后端完整回归 **35项**，前端 **6项**；原 V1 **16个文件指纹全部不变**。浏览器实测多步操作、刷新保留、缺时间弹窗、编辑和取消、未来日期每日提醒显示。
- 音频/图像使用测试素材验证，未偷开睡眠中的麦克风/摄像头；这不是实物识别准确率测评。

## 明早只剩实物边界需要联调

- **已阅读** ZIP 的 `docs/TEAM_UI_HANDOFF_2026-09-23.md`。ZIP 原 nRF 15秒停录已做兼容/修改稿；原手册明确实机 `haptic=false`。
- 硬件同学提供真实 USB 串口及摄像头 JPEG 地址，编译/核验连续录音与三击修改稿。当前电脑没有连接 nRF；ZIP 没有摄像头出图接口实现。
- 马达需先修复初始化，再实现固定3次震动（每次2秒、间隔2秒）与回执；Reminder 还需呼吸灯驱动。这些驱动**未在现有固件实现或实测**。软件接口已经备好，详见 `hardware/PROTOCOL-LIVE.md`。

## 打开与代码

- 双击 `start-live.command`；浏览器打开 **http://127.0.0.1:4173/phone-demo.html**。正式演示数据与4180测试库分开，首次为空，由真实输入创建。
- 新代码位于本文件所在的 `mindloop-connected/`，Git分支 `codex/live-capture-demo`，基于 GitHub `main@3a3a9b0`。用户已确认推送，GitHub 接入分支为 `codex/live-capture-demo`；尚未合并进 `main`。
- 启动/安装见 `README-LIVE.md`；API和硬件衔接见 `hardware/PROTOCOL-LIVE.md`。密钥、模型权重、数据库及测试音频未进入Git。
